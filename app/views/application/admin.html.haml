.cards
  .card
    .ribbon-wrapper
      .ribbon RIBBON
    .card-image
    = link_to(users_path) do
      .card-header Users
      .card-copy
        %p Tools for user management.
      .card-stats
        %ul
          %li
            = pluralize(User.find_each.size, "user")
            %span
              in database
