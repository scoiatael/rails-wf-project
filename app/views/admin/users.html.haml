%h1 Listing users
%table
  %thead
    %tr
      %th Email
      %th Name
      %th{:colspan => "2"}
  %tbody
    - User.find_each do |user|
      %tr
        %td= user.email
        %td= user.name
        %td= link_to 'Show', "user/#{user.id}"
        %td= link_to 'Destroy', "user/#{user.id}", method: :delete, data: { confirm: 'Are you sure?' }
